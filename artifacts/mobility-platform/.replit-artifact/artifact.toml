kind = "web"
previewPath = "/"
title = "Indo-German Mobility Corridor Platform"
version = "1.0.0"
id = "artifacts/mobility-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22817

[services.development]
run = "pnpm --filter @workspace/mobility-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mobility-platform", "run", "build" ]
publicDir = "artifacts/mobility-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22817"
BASE_PATH = "/"
